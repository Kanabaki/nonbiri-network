const db = require('../models');

// idk how to seed this...TO DO!
// need to import models for schema and then the users need to match the
// schema requirements
// I probably need an array with objects to seed
// Would I need reactions or maybe not? 